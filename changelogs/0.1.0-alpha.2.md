# Changelog 0.1.0-alpha.2

Build: `2026-01-02`

## 🚀 New Features

- **Image Support**: Added support for pasting images from clipboard and drag-and-drop with visual indicator (`9f1bb07`).
- **Export Chat History**:
  - Implemented chat history export to Markdown (`.md`) and JSON (`.json`) (`87dd044`).
  - Added native save dialog and auto-open file after export (`1213465`).
- **Streaming Timeout**: Added timeout mechanism for streaming messages with countdown timer (`12287b5`).

## 🐛 Bug Fixes

- **Chat Stability**: Fixed race condition when switching between chats rapidly (`f70149c`).
- **Error Handling**: Fixed UI crash when handling malformed JSON tool calls (`d26663c`).
- **Input Validation**:
  - Added message input length validation (`5bc595a`).
  - Added file upload validation for size and type (`8365235`).
- **UI State**: Fixed loading state persisting after stopping streaming (`4a43bfe`).

## ⚡ Improvements

- **Performance**:
  - Optimized message list rendering to reduce unnecessary re-renders (`eb99dab`).
  - improved textarea auto-resize logic for large inputs (`9d57eb9`).
- **UX**:
  - Simplified auto-scroll logic using `IntersectionObserver` (`6de111f`).
  - Smoothed message collapse/expand animations to prevent layout shifts (`98b5e4d`).
